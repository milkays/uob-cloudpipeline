
version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.7
    commands:
      - echo install
  pre_build:
    commands:
      - echo pre build
  build:
    commands:
      - echo build
  post_build:
    commands:
      - echo "Another post build 3"
      
  
artifacts:
  files:
    - template.yaml
  name: upbCodepipeline